# DreamJob — Система рекомендаций вакансий

**DreamJob** — это веб-приложение для парсинга вакансий с HeadHunter (hh.ru) и их рекомендации пользователям на основе их навыков и местоположения. Проект реализует полный цикл ETL: сбор данных, их очистку, хранение в PostgreSQL и построение персонализированных рекомендаций.

---

## 🚀 Основные возможности

- **Парсинг вакансий** с сайта hh.ru по двум категориям: профессиональные (IT) и общие (рабочие специальности).
- **Автоматическое обновление базы данных**: добавление новых вакансий и удаление устаревших.
- **Управление пользователями**: создание, просмотр и удаление пользователей, назначение им навыков и уровня (junior/middle/senior).
- **Рекомендательная система**: расчёт релевантности вакансии для пользователя на основе **коэффициента Жаккара** (сходство навыков) и города.
- **Веб-интерфейс** на Flask с возможностью просмотра вакансий, профилей пользователей и их рекомендаций.

---

## 🧠 Ключевые функции проекта

### 1. Сбор и парсинг данных (`parser.py`)
- Отправка GET-запросов к hh.ru с заданными поисковыми запросами и городами.
- Извлечение данных о вакансии: название, компания, опыт, зарплата, URL.
- Нормализация зарплаты (извлечение чисел и валюты).

### 2. Обновление базы данных (`db.py`)
- Вставка новых вакансий в таблицу `msod5.vacancies`.
- Автоматическое удаление вакансий, которые больше не встречаются в результатах парсинга.
- Добавление компаний, городов и навыков по мере необходимости.

### 3. Генерация пользователей (`create_users.py`)
- Генерация случайных пользователей (имя, фамилия, город, уровень).
- Назначение каждому пользователю случайного набора навыков.

### 4. Рекомендательная система (`recomendation.py`)
- Расчёт рекомендаций на основе пересечения навыков пользователя и вакансии.
- Использование взвешенной метрики:
score = 0.7 * jaccard(user_skills, vacancy_skills) + 0.3 * city_match
- Сохранение результатов в таблицу `msod5.recommendations`.

### 5. Веб-приложение (`app.py`)
- Flask-сервер с шаблонами HTML.
- Страницы: главная, список пользователей, профиль пользователя с рекомендациями.
- Возможность добавлять пользователей через модальное окно.

---

## 🗂️ Структура проекта

<img width="759" height="657" alt="image" src="https://github.com/user-attachments/assets/5f92f554-2d2a-48e7-b3de-bc7f29833fea" />


---

## ⚙️ Используемые технологии

- **Backend**: Python, Flask, psycopg2, BeautifulSoup, requests
- **База данных**: PostgreSQL
- **Фронтенд**: HTML, CSS, JavaScript (минимально)
- **Алгоритмы**: Коэффициент Жаккара (Jaccard similarity)

---

## 📦 Установка и запуск

1. Клонируй репозиторий:
   bash
   git clone https://github.com/Joriksss/recomendation.git
   cd dreamjob

3. Установи зависимости:
     pip install -r requirements.txt

3. Запусти парсинг и обновление вакансий:
     python db.py

4. Запусти веб-сервер:
     python app.py

5. Перейди по адресу:
    http://127.0.0.1:5000 | localhost

